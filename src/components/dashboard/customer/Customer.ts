export interface Customer {
    id: string;
    logo: string;          // Cambiado de 'avatar' a 'logo'
    nombre: string;        // Cambiado de 'name' a 'nombre'
    email: string;
    fechaAlta: string;       // Cambiado de 'createdAt' a 'fechaAlta'
    estado: string;        // Nuevo campo
    
  }